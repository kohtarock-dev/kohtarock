# =====================================================================
# 仕入れ元(ソース)設定
# ---------------------------------------------------------------------
# type ごとに必要なパラメータが異なります。詳細は README.md の
# 「ソースの追加方法」を参照してください。
#
# 共通パラメータ:
#   name               : 画面表示用の名前
#   enabled             : true/false でON/OFF
#   poll_interval_sec   : 巡回間隔(秒)。省略時は環境変数 DEFAULT_POLL_INTERVAL_SEC
#   keywords            : この中のいずれかを含む商品名だけを対象にする(省略時は default_keywords)
#   exclude_keywords    : この中のいずれかを含む商品名は除外する(省略時は default_exclude_keywords)
# =====================================================================

# エリアトラウト用品を拾うためのデフォルトキーワード。
# 自分の狙う銘柄・型番に合わせて自由に編集してください。
default_keywords:
  - エリアトラウト
  - トラウトロッド
  - トラウトスプーン
  - トラウトミノー
  - トラウトプラグ
  - ネイティブトラウト
  - 管理釣り場
  - 管釣り
  # 主要ブランド(適宜追加/削除)
  - ロデオクラフト
  - RODIO CRAFT
  - バルケイン
  - Valkein
  - アピア
  - APIA
  - ティモン
  - TIMON
  - 五三堂
  - フォレスト ルアー
  - Forest
  - ダイワ トラウト
  - シマノ トラウト
  - スミス パニック
  - ジャッカル トラウト
  - ima トラウト
  - リューギ トラウト

default_exclude_keywords:
  - 中古 ジャンク
  - 訳あり 破損

sources:
  # ---------------------------------------------------------------
  # 1) 楽天市場 商品検索API(公式・要APIキー)
  #    https://webservice.rakuten.co.jp/documentation/ichiba-item-search
  # ---------------------------------------------------------------
  - name: 楽天市場
    type: rakuten_api
    enabled: false  # 専門店のみ運用する方針のため無効化(要望があればtrueに戻すだけでOK)
    poll_interval_sec: 900
    genre_id: null   # 必要ならジャンルIDで絞り込み(釣具ジャンル等)
    sort: "-updateTimestamp"  # 更新が新しい順

  # ---------------------------------------------------------------
  # 2) Yahoo!ショッピング 商品検索API(公式・要APIキー)
  #    https://developer.yahoo.co.jp/webapi/shopping/v3/itemsearch.html
  # ---------------------------------------------------------------
  - name: Yahoo!ショッピング
    type: yahoo_shopping_api
    enabled: false  # 専門店のみ運用する方針のため無効化(未使用・APIキーも未設定)
    poll_interval_sec: 900

  # ---------------------------------------------------------------
  # 3) ヤフオク! 検索結果RSS(Yahoo!が公式に提供している検索結果のRSS配信機能を利用)
  #    例: https://auctions.yahoo.co.jp/closedsearch/closedsearch?p=<キーワード>&rss=1
  #    ※ 必ず自分のブラウザで検索URLを開き、RSSリンクの有無を確認してから有効化してください。
  # ---------------------------------------------------------------
  - name: ヤフオク! (トラウトロッド)
    type: rss
    enabled: false
    poll_interval_sec: 1800
    feed_url: "https://auctions.yahoo.co.jp/search/search?p=%E3%83%88%E3%83%A9%E3%82%A6%E3%83%88+%E3%83%AD%E3%83%83%E3%83%89&rss=1"

  # ---------------------------------------------------------------
  # 4) 釣具専門通販サイトの新着/入荷情報RSS
  #    多くの専門店はブログやお知らせページにRSSを持っています。
  #    サイトの<link rel="alternate" type="application/rss+xml">を確認して設定してください。
  # ---------------------------------------------------------------
  # トラウトアイランド(エリア/ネイティブトラウト専門店)。
  # robots.txtで許可された公式RSS(?mode=rss)を使用。
  # 扱っている商品のほぼ全てがトラウト関連のため match_all: true で
  # キーワード絞り込みをせず全件を新着として扱う(exclude_keywordsのみ適用)。
  - name: トラウトアイランド
    type: rss
    enabled: true
    poll_interval_sec: 1800
    feed_url: "http://troutisland.shop-pro.jp/?mode=rss"
    match_all: true

  # 越谷タックルアイランド・トラウト(エリアトラウト・渓流専門店)。
  # トラウトアイランドと同じColor Me Shop基盤で、robots.txtで許可された公式RSS。
  - name: 越谷タックルアイランド・トラウト
    type: rss
    enabled: true
    poll_interval_sec: 1800
    feed_url: "https://www.area-island.com/?mode=rss"
    match_all: true

  # t-Route(エリアトラウト用品専門店、東京・中野坂上)。
  # Shopifyの公式Atomフィード(全商品の新着順)。robots.txtで明示的にクロール許可されている。
  - name: t-Route
    type: rss
    enabled: true
    poll_interval_sec: 1800
    feed_url: "https://t-route.net/collections/all.atom"
    match_all: true

  # プロショップトモ(エリアトラウト専門店)。Color Me Shop基盤の公式RSS。
  - name: プロショップトモ
    type: rss
    enabled: true
    poll_interval_sec: 1800
    feed_url: "https://www.proshoptomo.com/?mode=rss"
    match_all: true

  # プロショップリバーロード(エリアトラウト専門店)。Color Me Shop基盤の公式RSS。
  - name: プロショップリバーロード
    type: rss
    enabled: true
    poll_interval_sec: 1800
    feed_url: "https://riverroad1091.shop/?mode=rss"
    match_all: true

  - name: ナチュラム 新着情報 (例)
    type: rss
    enabled: false
    poll_interval_sec: 1800
    feed_url: "https://www.naturum.co.jp/rss/new.rdf"

  # ---------------------------------------------------------------
  # 5) robots.txt を尊重した汎用HTMLポーリング(RSSが無いショップ向け)
  #    CSSセレクタで一覧ページから商品を抽出します。
  #    必ず対象サイトの利用規約・robots.txtを確認し、
  #    poll_interval_sec は短くしすぎない(30分以上推奨)でください。
  # ---------------------------------------------------------------
  - name: 専門店サイト (要セレクタ設定・サンプル)
    type: html_polite
    enabled: false
    poll_interval_sec: 1800
    list_url: "https://example-tackle-shop.example.com/new-arrivals"
    item_selector: "ul.item-list li.item"
    title_selector: ".item-title"
    price_selector: ".item-price"
    link_selector: "a"
    image_selector: "img"

  # マニアックス(エリアトラウト専門店)。RSS/Atomの配信が無いため、
  # 商品一覧ページ(新着順)をrobots.txt準拠でポーリングする。
  # (robots.txt自体が存在しないサイトで、標準仕様上は全許可扱いとなる)
  - name: マニアックス
    type: html_polite
    enabled: true
    poll_interval_sec: 1800
    list_url: "https://www.maniacs1091.jp/shopbrand/all_items/"
    item_selector: "ul.innerList li"
    title_selector: "p.name"
    price_selector: "p.price"
    link_selector: "a"
    image_selector: "img"
    match_all: true

  # タックルラウンジ(中古エリアトラウト専門の買取・販売店)。
  # WooCommerce製ショップの「新着」グリッドをrobots.txt準拠でポーリングする。
  # 中古品中心のため、状態の良い掘り出し物を安く仕入れられる可能性がある。
  - name: タックルラウンジ
    type: html_polite
    enabled: true
    poll_interval_sec: 1800
    list_url: "https://tackle-lounge.jp/shop/"
    item_selector: "li.arrival-card"
    title_selector: ".arrival-name"
    price_selector: ".arrival-price-main"
    link_selector: "a"
    image_selector: "img"
    match_all: true

  # プロショップザーキー(fishingshop-zarky.com)は robots.txt の取得自体が
  # Cloudflareのボット認証(JSチャレンジ)でブロックされるため、意図的に対応していません。
  # 自動巡回を強行するのはサイト側の意図に反するため、店のSNS等で手動確認してください。

  # ---------------------------------------------------------------
  # 6) フリマ/オークション(メルカリ・ラクマ・PayPayフリマ等)
  #    公式APIが一般提供されていないため、既定では無効化しています。
  #    有効化する場合は html_polite と同様にセレクタを設定し、
  #    必ず各サービスの利用規約を確認したうえで自己責任で運用してください。
  #    (過度な頻度でのアクセスは利用規約違反・アクセス遮断の対象になります)
  # ---------------------------------------------------------------
  - name: フリマ検索 (要セレクタ設定・既定OFF)
    type: html_polite
    enabled: false
    poll_interval_sec: 3600
    list_url: "https://example-flea-market.example.com/search?keyword=%E3%83%88%E3%83%A9%E3%82%A6%E3%83%88"
    item_selector: "ul.items li"
    title_selector: ".item-name"
    price_selector: ".item-price"
    link_selector: "a"
    image_selector: "img"
